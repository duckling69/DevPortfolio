[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Netlify has discontinued Node 18. Using 20 (current LTS) for best compatibility
  # with this project's older Vite 2 + React setup.
  NODE_VERSION = "20"

# Optional: Clear cache if you run into weird issues after changing Node version
# [build.processing]
#   skip_processing = false
